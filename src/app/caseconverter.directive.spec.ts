import { CaseconverterDirective } from './caseconverter.directive';

describe('CaseconverterDirective', () => {
  it('should create an instance', () => {
    const directive = new CaseconverterDirective();
    expect(directive).toBeTruthy();
  });
});
